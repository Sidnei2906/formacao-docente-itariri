document.querySelectorAll(".tabs button").forEach((button)=>button.addEventListener("click",()=>{document.querySelectorAll(".tabs button").forEach((item)=>item.classList.remove("active"));document.querySelectorAll(".tab-panel").forEach((panel)=>panel.classList.remove("active"));button.classList.add("active");document.getElementById(button.dataset.tab).classList.add("active")}));
document.querySelectorAll(".accordions article button").forEach((button)=>button.addEventListener("click",()=>{const detail=button.parentElement.querySelector("p");const open=detail.hidden;detail.hidden=!open;button.querySelector("b").textContent=open?"−":"+"}));
